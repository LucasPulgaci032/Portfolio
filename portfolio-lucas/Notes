/*criar uma escrita para orientar o leitor em qual sessão do prtfolio ele está


remover o gsap da div sobre e colocar na div de contatos


esquema para o scroll voltar ao topo quando a url trocar:  ScrollToTop.jsx
import { useEffect } from 'react';
import { useLocation } from 'react-router-dom';

function ScrollToTop() {
  const { pathname } = useLocation();

  useEffect(() => {
    Sempre que o pathname mudar, sobe pro topo
    window.scrollTo(0, 0);
  }, [pathname]);

  return null;
}

export default ScrollToTop;

instalar o nodemon

*/